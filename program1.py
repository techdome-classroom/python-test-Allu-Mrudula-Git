class Solution:
   
    def getTotalIsles(self, grid: list[list[str]]) -> int:
    #    write your code here
        n= len(grid)
        m= len(grid[0])
        ans= 0
        for i in range(n):
            for j in range(m):
                if grid[i][j]== 'L':
                    ans+=1
                    q= [(i,j)]
                    if()
                    
        return 0